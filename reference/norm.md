# Norm (L2) generic

Norm (L2) generic

L2 norm of a sparse_numeric vector

## Usage

``` r
norm(x, ...)

# S4 method for class 'sparse_numeric'
norm(x, ...)
```

## Arguments

- x:

  A sparse_numeric vector.

- ...:

  Ignored, included for consistency with the generic.

## Value

A numeric scalar containing the Euclidean norm.
