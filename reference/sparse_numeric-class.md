# sparse_numeric S4 Class

A sparse representation of a numeric vector storing only non-zero values
and their positions.

## Slots

- `value`:

  Numeric vector of non-zero values.

- `pos`:

  Integer vector of positions of those values.

- `length`:

  Integer, length of the full (dense) vector.
