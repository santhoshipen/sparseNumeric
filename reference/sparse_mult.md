# sparse_mult generic

sparse_mult generic

## Usage

``` r
sparse_mult(x, y, ...)

# S4 method for class 'sparse_numeric,sparse_numeric'
sparse_mult(x, y, ...)
```

## Arguments

- x, y:

  sparse_numeric vectors.

- ...:

  Ignored, included for consistency with the generic.

## Value

A sparse_numeric vector representing the element-wise product.
